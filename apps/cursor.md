https://github.com/ivstiv/cursor-version-manager

Cursor (VS Code fork) doesn't ship updates via system repositories; you have to install the AppImage for each update. CVM fixes this with `cvm --update`. It keeps all versions as you update—clear the cache/path when you need space.